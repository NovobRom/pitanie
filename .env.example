# Supabase (public values — safe to expose; access is enforced by RLS).
# The app falls back to these built-in defaults if the vars are unset, so
# setting them is optional. Override here or in Vercel project settings.
NEXT_PUBLIC_SUPABASE_URL=https://zigwrbvpeinczfwrvbiv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_OF3bYcNUiL4im8DMz_8e0A_A4SPnYfP
